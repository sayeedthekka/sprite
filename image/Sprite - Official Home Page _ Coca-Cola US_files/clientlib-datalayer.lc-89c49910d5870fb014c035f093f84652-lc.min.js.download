const CepDataLayer = {
    push: (args) => {
        if (window.adobeDataLayer) {
            adobeDataLayer.push(args);
        }
        if (window.dataLayer) {
            dataLayer.push(args);
        }
    }
}